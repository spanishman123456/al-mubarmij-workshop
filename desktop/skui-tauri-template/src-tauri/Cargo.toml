[package]
name = "skui-desktop"
version = "0.1.0"
description = "Minimal desktop host for an exported SKUI web application"
edition = "2021"
publish = false

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
